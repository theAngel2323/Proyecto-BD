# --- Conexión a MySQL ---
mysql-connector-python==8.3.0   # Driver oficial de MySQL para Python

# --- Variables de entorno ---
python-dotenv==1.0.1            # Carga el archivo .env automáticamente

# --- Seguridad y cifrado ---
bcrypt==4.1.3                   # Hashing de contraseñas (usuarios del sistema)
cryptography==42.0.5            # Cifrado AES para campos sensibles (DPI, diagnósticos)

# --- ETL y manejo de datos ---
pandas==2.2.2                   # Transformación de datos CSV/Excel
openpyxl==3.1.2                 # Lectura de archivos Excel (.xlsx)

# --- Interfaz gráfica (opcional, elegir uno) ---
# tkinter ya viene con Python estándar
customtkinter==5.2.2            # UI moderna sobre tkinter

# --- Logging y utilidades ---
colorlog==6.8.2                 # Logs con colores en consola

# --- Testing ---
pytest==8.2.1
pytest-mock==3.14.0